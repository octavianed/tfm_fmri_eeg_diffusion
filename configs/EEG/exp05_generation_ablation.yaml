# EEG Experiment 5 — final generative comparison: correct vs permuted vs zero.
_base_: [base.yaml]

experiment:
  name: exp05_eeg_generation_ablation

generation:
  # Where EEG Experiment 4 wrote its generated images / metadata.
  source_experiment: exp04_eeg_generation
  conditions: [correct, permuted, zero]
  compute_ssim: true        # requires scikit-image
  compute_lpips: true       # requires the lpips package
  num_case_examples: 5       # best / median / worst grids
